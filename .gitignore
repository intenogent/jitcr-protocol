# JITCR Protocol — GitHub whitelist
# Only the three published protocol files and this .gitignore are ever tracked.
# Everything else in this folder (archive, logs, working files) stays local only.

# Exclude everything by default
*

# Re-include only the published files
!.gitignore
!README.md
!INSTALL_PROMPT.md
!JITCR_Universal_Commands.md
